//! Raft + bootstrap configuration.
//!
//! Plan: Task 1 (stub) / Task 3 (timeouts + jitter for election tests).

// TODO(Task 1 / 3): pub struct SeedPeer { node_id, peer_id, addrs }
// TODO(Task 1 / 3): pub struct RaftConfig {
//     node_id,
//     voters / membership seed,
//     seed_peers: Vec<SeedPeer>,
//     election_timeout,
//     election_jitter,       // 0 in deterministic unit tests
//     heartbeat_interval,
//     rpc_timeout,
//     rpc_max_retries,
//     snapshot_threshold,
// }
